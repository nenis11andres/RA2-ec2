{% extends 'base.html.twig' %}

{% block title %}Clase{% endblock %}

{% block body %}
    <h1>Clase</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ clase.id }}</td>
            </tr>
            <tr>
                <th>Dia</th>
                <td>{{ clase.dia }}</td>
            </tr>
            <tr>
                <th>Hora</th>
                <td>{{ clase.hora }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_clase_index') }}">back to list</a>

    <a href="{{ path('app_clase_edit', {'id': clase.id}) }}">edit</a>

    {{ include('clase/_delete_form.html.twig') }}
{% endblock %}
